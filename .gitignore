# Secrets / per-machine config (created from the .sample files on each host)
website/config.php
agent/agent.conf

# Build output
agent/Agent.exe
agent/agentsvc.exe
agent/Agent.new.exe
agent/Agent.prev.exe
agent/*.pdb
agent/Embedded.cs
agent/agent.id
agent/worker.hb
agent/update.flag

# Runtime command queue (keep the folder + .htaccess, ignore live cmd/res files)
website/data/*/
website/data/*.json

# Local test scratch
_agenttest/

# chrome_nav_monitor PyInstaller build output (build the exe locally; commit the .py)
dist/
build_pyi/
chrome_nav_monitor.exe
__pycache__/

# OS / editor noise
Thumbs.db
.DS_Store
*.log
