# build output — keep the source AND the committed exe, ignore the rest
build/
build_pyi/
*.spec
__pycache__/
*.pyc
# Ship the compiled tool exe (see CLAUDE.md "Compiled tool exes ARE committed"):
dist/*
!dist/chnav.exe
